{%- block form_errors -%}
    {%- if errors|length > 0 -%}
        {%- for error in errors -%}
            class="error" data-bs-toggle="tooltip" data-placement="top" title="{{ error.message }}"
        {%- endfor -%}
    {%- endif -%}
{%- endblock form_errors -%}
