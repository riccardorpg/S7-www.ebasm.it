{% extends 'role/base.html.twig' %}

{% block home_path %}{{ path('notary_index') }}{% endblock %}

{% block role_menu %}
    {% set activeCompany = current_company() %}
    <li class="menu-item{{ app.request.attributes.get('_route') == 'notary_index' ? ' current' : '' }}">
        <a class="menu-link" href="{{ path('notary_index') }}"><div><i class="bi-speedometer2"></i> Scrivania</div></a>
    </li>

    {% if activeCompany %}
        {# Cambia agenzia subito sotto la scrivania #}
        <li class="menu-item">
            <a class="menu-link" href="{{ path('notary_deselect_agency') }}"><div><i class="bi-arrow-left-right"></i> Cambia agenzia</div></a>
        </li>

        {# Agenzia selezionata: label non cliccabile (stile "azienda attiva" di venanzieffe) #}
        <li class="menu-item"><hr class="d-none d-lg-block"></li>
        <li class="menu-item menu-agency-label">
            <span class="menu-link" style="cursor:default;">
                <div style="font-size:11px; letter-spacing:.6px; text-transform:uppercase; opacity:.6;">Agenzia attiva</div>
                <div class="d-flex align-items-center" style="gap:8px;">
                    <span class="badge bg-secondary">{{ activeCompany.code }}</span>
                    <div>{{ activeCompany.name }}</div>
                </div>
            </span>
        </li>

        <li class="menu-item{{ app.request.attributes.get('_route') starts with 'notary_practi' ? ' current' : '' }}">
            <a class="menu-link" href="{{ path('notary_practices') }}"><div><i class="bi-folder2-open"></i> Gestione pratiche</div></a>
        </li>
        <li class="menu-item"><hr class="d-none d-lg-block"></li>
    {% endif %}

    <li class="menu-item">
        <a class="menu-link" href="{{ path('logout') }}"><div><i class="bi-box-arrow-right"></i> Esci</div></a>
    </li>
{% endblock %}

{% block role_page_title_breadcrumb %}
    {% set crumb_current = block('role_page_title_inner')|striptags|trim %}
    <li class="breadcrumb-item">
        {% if app.request.attributes.get('_route') == 'notary_index' %}
            Notaio
        {% else %}
            <a href="{{ path('notary_index') }}">Notaio</a>
        {% endif %}
    </li>
    {% if app.request.attributes.get('_route') != 'notary_index' and crumb_current %}
        <li class="breadcrumb-item active" aria-current="page">{{ crumb_current }}</li>
    {% endif %}
{% endblock %}
