{% extends 'public/layout.html.twig' %}

{% block title %} - Accesso {{ company.name }}{% endblock %}
{% block robots %}<meta name="robots" content="noindex, follow">{% endblock %}

{% block content %}
    <section class="eb-section" style="padding-top:150px; min-height:70vh;">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-7 col-lg-5">
                    <div class="card border-0 shadow-sm" style="border-top:3px solid var(--eb-brand) !important;">
                        <div class="card-body p-4">
                            {# Branding agenzia #}
                            <div class="text-center mb-4">
                                <div class="badge bg-light text-dark mb-2">{{ company.code }}</div>
                                <h3 class="mb-0">{{ company.name }}</h3>
                                <p class="text-muted font_13">Area riservata agenzia</p>
                            </div>

                            {% if error %}
                                <div class="alert alert-danger alert-dismissible d-flex align-items-center">
                                    <i class="bi-exclamation-triangle-fill me-2"></i>
                                    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                </div>
                            {% endif %}

                            <form method="post" action="{{ path('login_check') }}">
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                <input type="hidden" name="codice" value="{{ company.code }}">

                                <div class="form-group mb-3">
                                    <label class="form-label">E-mail</label>
                                    <input type="email" name="email" value="{{ last_username }}" class="form-control" autofocus required>
                                </div>
                                <div class="form-group mb-3">
                                    <label class="form-label">Password</label>
                                    <input type="password" name="password" class="form-control" required>
                                </div>
                                <div class="d-flex align-items-center justify-content-between mb-3">
                                    <div class="form-check form-switch m-0">
                                        <input class="form-check-input" type="checkbox" role="switch" name="_remember_me" id="remember_me" checked>
                                        <label class="form-check-label font_13" for="remember_me">Rimani collegato</label>
                                    </div>
                                    <a href="{{ path('password_recover', {'codice': company.code}) }}" class="font_13">Recupera password</a>
                                </div>
                                <button type="submit" class="button button-3d button-rounded button-large m-0 w-100" style="background:var(--eb-brand);">Accedi</button>
                            </form>
                        </div>
                    </div>
                    <p class="text-center mt-3"><a href="{{ path('login') }}" class="font_13 text-muted">&larr; Cambia codice</a></p>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
